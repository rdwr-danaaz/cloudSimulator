[pytest]
# Ensure the repository root is importable so tests can `import cloud_mock_server`
# regardless of how pytest is launched (bare `pytest` vs `python -m pytest`).
pythonpath = .
testpaths = tests

